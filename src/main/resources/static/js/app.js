/*global console*/
/* eslint no-console: "off" */

function randPAO(Number) {
    var pNum = Number.slice(0,2);
    var aNum = Number.slice(3,5);
    var oNum = Number.slice(6,8);
    console.log(pNum);
    
}

randPAO(12345);